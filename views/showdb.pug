extends layout

block content
  h1 ShowDB
  table(border = 1)
    br
    th ชื่อ 
    br
    th นามสกุล
    br
    th username
    br
    th password
    br
    th email
    for item in data
        tr
            td #{item.firstname}
            td #{item.lastname}
            td #{item.username}
            td #{item.password}
            td #{item.email}